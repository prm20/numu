# Numu Backend – Current State & Setup Log  
*(as of Mon 16 Feb 2026, ~22:30 PT)*

This file documents:

- Where your Django/Postgres setup currently stands
- What we changed/fixed
- Every important command you ran, what it did, and which error it was addressing
- The current project structure (approximate, based on what we’ve seen)

You can drop this into the repo as something like `docs/backend-setup-notes.md` or `notes/2026-02-16-setup.md`.

---

## 1. High‑level status

### Backend / Django

- Django project is in `backend/`.
- App `numu` is installed and its migrations have been applied.
- `settings.DATABASES` reads DB configuration from your `.env` file.
- Django admin is running and accessible at:  
  `http://127.0.0.1:8000/admin/`
- The following `numu` models are registered and visible in admin:
  - `Disc`
  - `Lesson`
  - `Lexeme`
  - `AudioFile`

### PostgreSQL

- Database name: `numu_db`
- DB user: `pmatson`
- DB password: `HGsAYusmidGWbFjkFAD6` (stored in `.env`)
- `numu_db` has the `vector` extension installed:
  - Used by `Lexeme.embedding = VectorField(dimensions=1536, ...)`
- Django migrations are fully applied:

  ```bash
  python manage.py migrate
